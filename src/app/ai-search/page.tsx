import { redirect } from "next/navigation";

export const metadata = {
  title: "AI Search Visibility — Customers.Direct",
  robots: { index: false },
};

export default function AISearchPage() {
  redirect("/");
}
